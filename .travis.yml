dist: xenial

language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"

matrix:
  fast_finish: false

install:
  - pip install -r requirements-dev.txt
  - pip install .

script:
  - pytest --cov

after_success:
  - codecov -X gcov
